# AI 友好企业官网自动生成平台技术设计报告

## 1. 项目定位

本项目不建议继续定义为普通的 Website Builder。普通建站工具的核心是“快速生成一个看起来像官网的网站”，但我们的优势来自已有的 AWA 项目：可以诊断网站是否容易被大模型抓取、理解和引用。

因此本项目应定位为：

> 面向中小企业的 AI 友好官网自动生成平台。用户上传企业资料后，系统自动生成结构清晰、内容可读、证据充分、适合搜索引擎和大模型抓取理解的企业官网。

核心目标不是替代设计师做漂亮网页，而是帮助企业解决三个问题：

- 官网能否被正常抓取。
- 大模型能否看懂企业是谁、卖什么、服务谁。
- 网站内容是否有足够证据支撑模型在用户提问时引用或推荐。

这个方向和 AWA 平台形成闭环：

```text
AWA 诊断现有官网问题
    ↓
WBS 生成 AI 友好官网
    ↓
AWA 自动复检
    ↓
根据评分和建议继续优化
```

## 2. 产品边界

### 2.1 平台要做什么

- 企业资料上传和结构化整理。
- 行业识别和内容缺口检查。
- 自动生成网站栏目、页面结构和文案。
- 自动生成 Title、Meta Description、H1/H2、正文、FAQ、案例、产品页。
- 自动生成 Organization、WebSite、Product、Service、FAQPage 等 JSON-LD。
- 自动生成 sitemap.xml、robots.txt、canonical、hreflang 基础配置。
- 提供可视化编辑和人工审核。
- 一键发布静态或 SSR 官网。
- 发布前调用 AWA 进行 AI 可见性验收。

### 2.2 平台不应该承诺什么

- 不承诺真实搜索排名。
- 不承诺任何大模型一定收录、引用或推荐。
- 不自动伪造客户案例、资质、证书或新闻。
- 不把模型生成内容直接发布，必须有用户确认和证据来源。

## 3. 核心产品流程

```text
用户上传资料
    ↓
资料解析与企业画像生成
    ↓
内容完整度检查
    ↓
行业模板与页面结构推荐
    ↓
AI 生成初稿
    ↓
证据绑定与人工确认
    ↓
生成 AI 友好网页
    ↓
调用 AWA 预检
    ↓
通过阈值后发布
    ↓
定期复检和迭代
```

## 4. 系统总体架构

```text
前端控制台
    ├── 资料上传
    ├── 网站编辑器
    ├── 生成进度
    ├── AWA 预检报告
    └── 发布管理

后端业务服务
    ├── 用户与租户
    ├── 企业资料库
    ├── 内容资产库
    ├── 模板中心
    ├── 页面生成服务
    ├── 发布服务
    └── 审核与版本管理

AI 服务层
    ├── 资料抽取
    ├── 行业识别
    ├── 内容缺口分析
    ├── 文案生成
    ├── FAQ 生成
    └── 结构化数据生成

质量验收层
    ├── AWA 静态抓取检测
    ├── AWA 浏览器渲染检测
    ├── AWA 多模型理解分析
    ├── AWA 确定性评分
    └── 发布门禁

官网交付层
    ├── 静态站点
    ├── SSR 渲染
    ├── CDN
    ├── HTTPS
    └── 域名绑定
```

## 5. 推荐技术栈

### 5.1 管理端前端

推荐：

- React 或 Vue3
- TypeScript
- Vite
- TailwindCSS
- shadcn/ui 或 Element Plus

建议优先用 React，原因是 AWA Web 端已经是 React，后续可以共享组件、报告展示方式和任务状态交互逻辑。

### 5.2 官网渲染层

推荐两种模式：

```text
MVP：静态站点生成
后续：SSR/SSG 混合渲染
```

MVP 优先使用静态生成，原因：

- 抓取稳定。
- 部署简单。
- 成本低。
- 不依赖登录态和客户端 JS。
- 更符合“AI 友好官网”的目标。

可选技术：

- Astro
- Next.js SSG
- Nuxt SSG
- 自研 HTML 模板渲染器

建议 MVP 不要上来就做复杂 Nuxt SSR。企业官网多数页面是内容型页面，静态生成足够，并且更容易被爬虫读取。

### 5.3 后端

如果团队偏 Java：

- Spring Boot 3
- Spring Security
- MyBatis Plus
- MySQL
- Redis
- MinIO

如果希望更快和 AWA 集成：

- FastAPI
- SQLAlchemy
- PostgreSQL 或 MySQL
- Redis
- MinIO

建议第一版可以用 FastAPI，原因：

- AWA 已经是 Python/FastAPI 方向。
- 资料抽取、LLM 调用、模板生成和 AWA 复检更容易集成。
- MVP 速度更快。

后续如果要企业级后台、权限体系和复杂 SaaS 计费，再考虑 Java 主服务。

### 5.4 AI 调用层

模型调用不要散落在业务代码中，统一封装：

```text
ai/
  llm_client.py
  prompts/
  extractors/
  generators/
  validators/
```

支持：

- OpenClaw / ZeroToken 网页模型。
- API 模型。
- 多模型生成或多模型复核。

重要原则：

- 模型负责理解和生成。
- 程序负责规则、校验、发布门禁。
- 生成内容必须绑定输入资料或用户确认。

## 6. 核心数据模型

### 6.1 企业资料 CompanyProfile

```json
{
  "company_name": "企业名称",
  "legal_name": "工商全称",
  "industry": "行业",
  "description": "公司介绍",
  "founded_year": "成立年份",
  "location": "所在地区",
  "service_area": ["广东", "广西"],
  "advantages": ["优势1", "优势2"],
  "certificates": ["资质1"],
  "contact": {
    "phone": "",
    "email": "",
    "address": ""
  }
}
```

### 6.2 产品服务 ProductService

```json
{
  "name": "产品名称",
  "category": "产品分类",
  "description": "产品说明",
  "parameters": {
    "规格": "",
    "用途": ""
  },
  "applications": ["应用场景"],
  "target_customers": ["目标客户"],
  "evidence_files": ["来源资料ID"]
}
```

### 6.3 证据资产 EvidenceAsset

```json
{
  "id": "asset_001",
  "type": "pdf/image/text/certificate/case",
  "filename": "资质证书.pdf",
  "extracted_text": "提取后的文本",
  "verified_by_user": true,
  "allowed_for_publish": true
}
```

### 6.4 页面 PageDraft

```json
{
  "page_type": "home/about/product/contact/faq/case",
  "slug": "/products/oxygen",
  "title": "页面 Title",
  "meta_description": "页面摘要",
  "h1": "主标题",
  "sections": [],
  "json_ld": [],
  "status": "draft/reviewed/published"
}
```

## 7. 模板系统设计

模板不应该只是视觉模板，而应该是“信息架构模板”。

每个模板必须定义：

- 页面列表。
- 每个页面必须包含的内容字段。
- 每个区块的结构化语义。
- 对应的 JSON-LD 类型。
- AWA 验收规则。

### 7.1 模板层级

```text
行业模板
    ↓
页面模板
    ↓
区块模板
    ↓
组件模板
    ↓
字段协议
```

### 7.2 页面建议

企业官网 MVP 至少生成：

- 首页
- 关于我们
- 产品/服务列表
- 产品/服务详情
- 案例或应用场景
- FAQ
- 联系我们
- 隐私政策
- 服务条款

这些页面不是为了“看起来完整”，而是为了让模型能稳定识别企业主体、业务范围、可信度和推荐证据。

### 7.3 模板 JSON 示例

```json
{
  "template_id": "b2b_industrial_v1",
  "industry": "B2B 工业企业",
  "required_pages": [
    "home",
    "about",
    "products",
    "product_detail",
    "applications",
    "faq",
    "contact",
    "privacy",
    "terms"
  ],
  "page_rules": {
    "home": {
      "required_fields": ["company_name", "industry", "main_products", "service_area"],
      "required_schema": ["Organization", "WebSite"],
      "min_text_length": 800
    },
    "product_detail": {
      "required_fields": ["product_name", "description", "parameters", "applications"],
      "required_schema": ["Product"],
      "min_text_length": 600
    },
    "faq": {
      "required_fields": ["questions"],
      "required_schema": ["FAQPage"],
      "min_question_count": 5
    }
  }
}
```

## 8. AI 内容生成设计

### 8.1 资料解析

输入来源：

- 用户手动填写。
- Word/PDF。
- 产品表格。
- 企业宣传册。
- 资质证书图片。
- 旧官网 URL。

输出：

- 企业画像。
- 产品服务列表。
- 案例和应用场景。
- 资质与可信度材料。
- 缺失字段清单。

### 8.2 内容生成原则

AI 可以润色，但不能编造关键事实。

必须区分：

```text
用户提供事实
AI 推断内容
AI 建议补充
禁止发布内容
```

例如：

- 公司成立年份没有资料，不允许编造。
- 资质证书没有上传，不允许生成“拥有多项国家认证”。
- 客户案例没有证据，可以生成“建议补充客户案例”，不能生成虚假客户名称。

### 8.3 生成内容类型

- 首页价值主张。
- 公司介绍。
- 产品详情。
- 服务流程。
- 应用场景。
- FAQ。
- SEO 标题和摘要。
- 图片 Alt。
- JSON-LD。
- sitemap 和 robots 建议。

## 9. AI 友好页面生成标准

每个生成页面必须满足以下规则：

### 9.1 抓取可访问性

- 页面不依赖登录。
- 不使用 CAPTCHA 阻断公开内容。
- 不把主要正文放在图片里。
- 不把核心内容只放在前端异步接口中。
- 重要页面必须返回 200。
- 避免错误重定向链。

### 9.2 文本可读性

- 每页必须有唯一 Title。
- 每页必须有 Meta Description。
- 每页必须有且只有一个主 H1。
- H2/H3 用于清晰表达内容层级。
- 首页建议不少于 800 字符。
- 产品详情页建议不少于 600 字符。
- FAQ 至少 5 个问题。

### 9.3 企业实体清晰度

必须清楚出现：

- 企业名称。
- 行业。
- 所在地区。
- 主营产品/服务。
- 服务范围。
- 联系方式。
- 关于我们入口。

### 9.4 产品服务完整度

产品/服务页面必须包含：

- 名称。
- 分类。
- 简介。
- 参数或规格。
- 使用场景。
- 适用客户。
- 交付方式或服务流程。
- 常见问题。

### 9.5 可信度信号

建议生成或要求用户补充：

- 资质证书。
- 案例。
- 团队介绍。
- 工厂/办公/服务现场图片。
- 联系方式。
- 隐私政策。
- 服务条款。

### 9.6 结构化数据

至少支持：

- Organization
- WebSite
- Product
- Service
- FAQPage
- BreadcrumbList

生成后必须进行 JSON 格式校验，字段缺失时给出提示。

## 10. AWA 集成设计

WBS 不应该自己重新实现一套诊断体系，而应该复用 AWA。

### 10.1 集成方式

```text
WBS 生成预览站
    ↓
调用 AWA CLI/API
    ↓
获得 result.json + summary.md
    ↓
读取五维评分和问题列表
    ↓
展示给用户或阻止发布
```

### 10.2 发布门禁

建议第一版发布阈值：

```text
总分 >= 70
抓取可访问性 >= 20
可读文本质量 >= 14
企业实体清晰度 >= 14
产品服务完整度 >= 12
AI 推荐潜力 >= 9
```

如果未达标：

- 允许保存草稿。
- 不建议一键发布。
- 展示具体问题。
- 提供“一键补齐建议”。

### 10.3 复检闭环

```text
生成初稿
    ↓
AWA 诊断
    ↓
读取扣分项
    ↓
生成修改建议
    ↓
用户确认
    ↓
重新生成
    ↓
再次诊断
```

## 11. 数据库设计

### 11.1 核心表

```text
tenant
user
company_profile
evidence_asset
product_service
site_project
site_page
page_section
template
template_version
generation_job
audit_job
publish_job
domain_binding
```

### 11.2 关键字段

`site_project`：

- id
- tenant_id
- company_id
- name
- template_id
- status
- preview_url
- published_url
- latest_audit_score
- created_at
- updated_at

`site_page`：

- id
- project_id
- page_type
- slug
- title
- meta_description
- h1
- content_json
- html_snapshot
- json_ld
- status

`audit_job`：

- id
- project_id
- awa_run_id
- status
- score
- report_path
- result_path
- issues_json
- created_at

## 12. 发布系统设计

MVP 推荐静态发布：

```text
页面 JSON
    ↓
HTML 生成
    ↓
静态资源打包
    ↓
生成 sitemap.xml / robots.txt
    ↓
部署到 Nginx 或对象存储
    ↓
绑定域名和 HTTPS
```

上线前检查：

- 首页可访问。
- 所有内部链接不返回 404。
- sitemap 包含重要页面。
- robots 不误伤常见爬虫。
- AWA 评分达到发布阈值。

## 13. 管理端页面设计

建议管理端分为 5 个主页面：

### 13.1 新建网站

- 上传企业资料。
- 填写企业基础信息。
- 选择行业。
- 选择模板风格。
- 开始生成。

### 13.2 资料中心

- 企业资料。
- 产品服务。
- 案例资质。
- 图片素材。
- 缺失资料提醒。

### 13.3 网站编辑器

- 页面列表。
- 区块编辑。
- 文案编辑。
- 图片替换。
- SEO 字段编辑。
- JSON-LD 预览。

### 13.4 AI 可见性检查

- AWA 总览评分。
- 五维评分细则。
- 抓取问题。
- 内容缺口。
- 模型理解结果。
- 一键生成修复建议。

### 13.5 发布中心

- 预览地址。
- 发布状态。
- 域名绑定。
- HTTPS 状态。
- 历史版本回滚。

## 14. 项目目录建议

```text
WBS_Dev/
  backend/
    app/
      api/
      models/
      services/
      generators/
      templates/
      integrations/
        awa_client.py
        llm_client.py
  web/
    src/
      pages/
      components/
      editors/
      audit/
  renderer/
    templates/
    components/
    build_site.py
  docs/
  outputs/
  tests/
```

如果后续和 AWA 放在一个大项目中，可以升级为：

```text
AI_GEO_Platform/
  AWA_Dev/
  WBS_Dev/
  shared/
    llm/
    auth/
    task_queue/
    report_components/
```

## 15. MVP 开发计划

### Phase 0：项目骨架

- 建立 WBS 项目目录。
- 确定技术栈。
- 建立基础 API。
- 建立前端控制台。
- 建立页面 JSON 协议。

### Phase 1：资料录入和结构化

- 企业资料表单。
- 产品服务表单。
- 文件上传。
- 资料解析。
- 缺失字段检查。

### Phase 2：静态网站生成

- 首页模板。
- 关于我们模板。
- 产品列表和详情页。
- 联系我们页。
- FAQ 页。
- sitemap.xml。
- robots.txt。
- JSON-LD。

### Phase 3：AI 生成

- 企业介绍生成。
- 产品描述生成。
- FAQ 生成。
- SEO 字段生成。
- 图片 Alt 生成。
- 内容事实校验。

### Phase 4：AWA 预检闭环

- 调用 AWA API/CLI。
- 展示评分和问题。
- 根据扣分项生成修改建议。
- 发布门禁。

### Phase 5：发布和版本管理

- 预览站点。
- 静态发布。
- 域名绑定。
- HTTPS。
- 版本回滚。

## 16. 第一版建议做法

第一版不要一开始做完整 SaaS。建议先做本地或内网 MVP：

```text
用户输入资料
    ↓
生成静态官网目录
    ↓
本地预览
    ↓
调用 AWA 检查
    ↓
导出网站包和诊断报告
```

原因：

- 能最快验证商业价值。
- 能复用 AWA 已完成能力。
- 避免一开始陷入支付、租户、权限、部署平台等复杂问题。
- 客户最关心的是“我的网站生成后能不能被 AI 看懂”，不是后台功能有多复杂。

## 17. 关键风险

### 17.1 AI 编造事实

解决：

- 所有关键事实必须来自用户资料。
- 生成结果标记来源。
- 发布前人工确认。

### 17.2 网站好看但不可抓取

解决：

- MVP 优先静态生成。
- 主体内容直接写入 HTML。
- 每次发布前跑 AWA。

### 17.3 模板同质化

解决：

- 模板关注行业信息架构。
- 视觉风格可配置。
- 文案和栏目根据资料动态生成。

### 17.4 报告承诺过度

解决：

- 只说 AI 可见性、可读性、证据完整度。
- 不承诺排名、收录和推荐概率。

## 18. 总结

WBS 的核心不应该是再做一个普通建站工具，而是做一个能和 AWA 联动的“AI 友好官网生成系统”。

最终产品价值是：

```text
用户给资料
系统生成网站
AWA 验证网站是否容易被 AI 抓取和理解
系统根据诊断结果继续优化
最终交付一套结构清晰、证据充分、机器可读的企业官网
```

这会比传统模板建站更有差异化，也更符合当前企业对 AI 搜索、AI 推荐和大模型检索可见性的实际需求。
