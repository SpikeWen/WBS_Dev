# 开发进度记录

## 2026-08-07

### 已完成

- 读完现有设计文档和仓库结构。
- 确认第一步先做后端最小骨架，而不是先上复杂前端或数据库迁移。
- 搭建了 `backend/` 的 FastAPI 项目壳子。
- 建了站点领域实体、应用服务、内存仓储和基础 API。
- 补了一份后端运行说明。
- 已用应用内直调验证站点创建、查询、更新和状态接口。
- 接入 SQLite 持久化仓储，站点、站点身份、企业档案会保存到本地数据库。
- 补齐站点列表、站点身份、企业档案和空壳官网预览接口。
- 搭建 `web/` 的 React + TypeScript + Vite 前端骨架。
- 完成站点驾驶舱 MVP：创建站点、选择站点、编辑身份、编辑企业档案、嵌入预览。
- 前端已通过 `npm run build`。
- 新增固定页面实体、页面树接口和 SQLite 持久化表。
- 前端新增固定页面创建、选择、编辑、状态和排序维护。
- 空壳官网预览已经能渲染后台保存的固定页面正文。
- 新增最小后端测试，覆盖站点、企业档案、固定页面、文章和预览链路。
- 新增文章实体、文章接口和 SQLite 持久化表。
- 前端新增文章创建、选择、编辑、分类、摘要、正文和状态维护。
- 空壳官网预览已经能展示后台保存的文章内容。
- 新增产品资料实体、产品接口和 SQLite 持久化表。
- 前端新增产品创建、选择、编辑、分类、型号、摘要、介绍、规格参数、价格说明和状态维护。
- 空壳官网预览已经能展示后台保存的产品资料。
- 后端 MVP 测试已扩展到产品链路。
- 新增 FAQ 实体、FAQ 接口和 SQLite 持久化表。
- 前端新增 FAQ 创建、选择、编辑、分类、排序、回答和状态维护。
- 空壳官网预览已经能展示后台保存的 FAQ 内容。
- 后端 MVP 测试已扩展到 FAQ 链路。
- 新增案例实体、案例接口和 SQLite 持久化表。
- 前端新增案例创建、选择、编辑、客户、行业、挑战、方案、结果、项目时间和状态维护。
- 空壳官网预览已经能展示后台保存的案例内容。
- 后端 MVP 测试已扩展到案例链路。
- 新增服务项目实体、服务接口和 SQLite 持久化表。
- 空壳官网预览已经能展示后台保存的服务项目内容。
- 新增最小发布功能：可将当前预览 HTML 输出到 `exports/{site_id}/{version}/index.html`。
- 新增发布记录接口，后端 MVP 测试已覆盖发布链路。
- 新增媒体资产实体、媒体接口和 SQLite 持久化表。
- 前端新增服务项目编辑区，服务内容可创建、选择、编辑、发布/隐藏并进入前台预览。
- 前端新增媒体素材上传、素材列表、素材 URL 和 Alt 文本维护。
- 前端新增发布按钮和发布历史记录，发布后的静态页面可从后台打开查看。
- 后端 MVP 测试已扩展到媒体资产链路。
- 新增固定页面、文章、产品、FAQ、案例、服务项目和媒体素材的删除接口。
- 前端新增各内容模块删除按钮，删除后会重新加载当前站点并刷新预览。
- 前端 Logo、Favicon、文章/产品/案例封面图输入框已支持从已上传媒体 URL 中选择。
- 后端 MVP 测试已扩展到内容删除链路。
- 前台预览新增导航栏、Logo 渲染和文章/产品/案例封面图渲染。
- 固定页面新增独立预览地址：`/api/sites/{site_id}/preview/pages/{slug}`。
- 发布功能现在会输出首页和每个固定页面的静态 HTML。
- 前端预览面板新增“当前页面”入口，编辑固定页面时可直接打开独立预览。
- 文章、产品、案例、服务新增独立详情预览地址。
- 发布功能现在会输出文章、产品、案例、服务的静态详情页。
- 前端预览面板新增文章详情、产品详情、案例详情、服务详情快捷入口。
- 新增 `SiteContent` 站点内容包，由应用层统一组装站点、档案和各内容模块。
- 新增基础设施层 `site_preview_renderer.py`，前台预览 HTML 渲染从 API 路由中抽离。
- 发布流程改为直接使用预览渲染器输出静态 HTML，避免通过路由响应对象中转。
- 新增基础设施层 `site_preview_theme.py`，预览文档壳子和样式已从区块渲染中分离。
- `site_preview_renderer.py` 已进一步拆分页面、文章、产品、FAQ、案例、服务的列表卡片渲染函数。
- 文章、产品、案例、服务详情页已拆成各自独立渲染函数。
- 修复内容详情页错误显示“首页”激活态的问题，并补充回归测试。
- 前端新增共享 `Field` 组件。
- 前端已抽离 `MediaPanel`、`PublishPanel`、`PreviewPanel`，组件只接收状态和事件，不直接调用 API。
- `App.tsx` 已移除媒体、发布、预览三块大段 JSX，前端构建验证通过。
- 前端新增 `ContentWorkbenchPanel` 通用内容工作台壳子。
- 前端已抽离 `ArticlePanel` 和 `ProductPanel`，文章/产品编辑区不再堆在 `App.tsx` 中。
- `App.tsx` 已降到约 1800 行，前端构建验证通过。
- 前端新增 `FaqPanel`、`CasePanel`、`ServicePanel`，FAQ/案例/服务编辑区也已拆出。
- `App.tsx` 已降到约 1400 行，前端构建验证通过。
- 前端新增 `SiteIdentityPanel`、`PagePanel`、`CompanyProfilePanel`。
- 站点身份、页面树/固定页面、企业档案三块基础资料编辑区已从 `App.tsx` 抽离。
- `App.tsx` 已降到约 1160 行，前端构建验证通过。
- 前端新增共享 `toSlug` 工具，页面、文章、产品、案例、服务的 Slug 自动生成规则已统一。
- 前端新增 `AssetUrlInput` 素材 URL 选择组件。
- 站点 Logo、Favicon、文章封面、产品封面、案例封面字段已支持从已上传图片素材中点击选择，同时保留手动输入 URL。
- 新增发布前内容检查领域模型和接口：`GET /api/sites/{site_id}/publish-readiness`。
- 发布前检查会提示站点身份、企业档案、固定页面和各内容模块的基础缺口，但不会阻止发布。
- 前端发布面板已展示发布前检查结果，并保持发布按钮可用。
- 前端新增 `SiteDashboardPanel` 站点驾驶舱。
- 驾驶舱已展示站点状态、发布检查摘要、最近发布版本、各内容模块总量和已发布数量，并提供前台预览入口。
- 固定页面新增 `show_in_nav` 导航显示控制字段。
- 页面编辑器新增“显示在前台导航”开关；关闭后页面仍可预览和发布，但不会出现在前台导航中。
- SQLite 初始化已补轻量列迁移，已有本地库会自动添加 `pages.show_in_nav`。
- 前端保存动作已补最小表单校验，避免站点名称、模板 ID、标题、名称、Slug、FAQ 问题等关键字段被保存为空。
- 创建和保存关键字段时会进行 `trim()` 归一化，减少前后空格造成的无效数据。
- 前端 API 错误解析已优化，FastAPI 校验错误会显示更友好的字段级消息。
- 新增 `DuplicateSlugError` 领域错误。
- SQLite 仓储会将页面、文章、产品、案例、服务的 slug 唯一冲突转换为领域错误。
- FastAPI 已将重复 slug 转换为 `409 Conflict` 和友好错误消息。
- 前端新增统一的 `refreshSiteDerivedState`，保存或创建内容后会同步刷新前台预览版本和发布前检查结果。
- 站点驾驶舱和发布面板的检查摘要不再只依赖重新加载站点，内容保存后会及时更新。
- 后台工作区已从“所有面板纵向堆叠”改为模块页式导航。
- 右侧站点模块现分为驾驶舱、站点身份、企业档案、页面、文章、产品、案例、服务、FAQ、媒体、发布、预览。
- 站点驾驶舱新增工作流入口，会根据站点身份、企业档案、固定页面、服务内容和发布检查状态提示下一步。
- 已通过 API 新建纯文字测试站点 `WBS 文字测试官网`，不上传图片，仅录入站点身份、企业档案、固定页面、文章和服务内容。
- 纯文字测试站点前台预览已确认能显示后台保存的文字内容。
- 各编辑模块新增对应预览入口：站点身份、企业档案、固定页面、文章、产品、案例、服务、FAQ 均可从当前模块直接打开对应前台预览。
- 模块导航中的预览入口已明确为“总体预览”，用于查看完整官网预览。
- 对应预览入口已从新窗口外链改为当前后台窗口内预览。
- 仅保留有明确对应前台位置的模块预览；FAQ 暂不提供独立词条预览，避免把总体预览误当成单条 FAQ 预览。
- `Field` 组件新增字段说明能力。
- 站点身份、企业档案、页面和核心内容模块的 URL 别名、SEO、封面图等易混字段已补充更清晰的名称和说明。
- 新增聚焦预览接口：`/api/sites/{site_id}/preview/focus/{section}` 和 `/api/sites/{site_id}/preview/focus/{section}/{slug}`。
- 模块内预览已改为聚焦预览，只渲染当前模块对应的官网片段或详情内容，不再加载完整官网页面。
- `Field` 组件新增字段状态徽标：必填、选填、只读、系统。
- 创建站点、站点身份、企业档案、页面、文章、产品、案例、服务、FAQ、媒体素材表单已开始标注哪些字段需要用户填写、哪些字段可以选填、哪些字段由系统固定显示。
- 媒体素材的文件名、类型、大小、访问 URL 已标为只读/系统字段，避免用户误以为需要手动填写。
- Logo、Favicon、文章/产品/案例封面图控件已从“填写图片地址”改为“选择图片”。
- 图片字段现在显示当前选择、素材缩略图列表、清除按钮，并将手动图片地址折叠为高级备用入口。
- “URL 别名”统一改为“页面短链接”，说明系统会自动生成，通常不需要用户修改。
- 媒体素材的“访问 URL”改为“系统链接”，减少普通用户误操作。
- 媒体素材上传流程已去掉顶部“保存素材”二次动作；上传成功即完成保存。
- 媒体素材说明改为独立“更新说明”动作，只有修改说明时才需要点击。
- 后台表单、模块导航、工作台、媒体列表等间距已收紧，减少板块之间过大的空白割裂。
- 前台预览模板已放宽到 1180px 容器，Hero 和内容卡片改为更开阔的官网式布局。
- 前台预览去掉“空壳官网预览”和站点状态等测试感文案。

### 当前状态

- 现在具备一个可演示的 MVP：后台资料保存后，空壳官网预览会读取最新内容。
- 固定页面内容已经进入 MVP 主链路。
- 文章内容已经进入 MVP 主链路。
- 产品资料已经进入 MVP 主链路。
- FAQ 已经进入 MVP 主链路。
- 案例已经进入 MVP 主链路。
- 服务项目已经进入 MVP 主链路。
- 发布记录和静态输出已经进入 MVP 功能链路。
- 媒体素材管理已经进入 MVP 功能链路。
- 前端已具备从建站、填资料、维护内容、上传素材、预览到发布的基础闭环。
- 内容模块已具备新增、编辑、状态维护和删除能力。
- 前台预览从单页汇总推进到具备基础导航和固定页面独立输出。
- 前台预览已经具备首页、固定页面和核心内容详情页的基础浏览结构。
- 前台模板渲染已从 API 路由抽到基础设施层，路由层重新回到较薄的请求编排角色。
- 前台渲染器的主流程已与各内容区块渲染函数分离，后续替换单个模块模板更容易。
- 前端开始按面板边界拆分，媒体、发布、预览已不再堆在主组件中。
- 文章和产品已开始复用通用内容工作台结构。
- FAQ、案例、服务也已复用同一工作台边界。
- 站点身份、固定页面和企业档案也已独立成面板组件，主组件主要保留状态、数据加载和应用层事件编排。
- Slug 自动生成规则已抽为共享前端工具，后续调整 URL 规则不需要逐个内容模块修改。
- 媒体素材已从“复制 URL 再粘贴”的弱交互，推进到核心图片字段内可直接选择已上传素材。
- 发布链路新增非强制内容检查能力，用于运营人员发现缺项，不改变主流程可发布逻辑。
- 第一阶段中的“站点驾驶舱”已补齐基础版本，后台首页面板不再只是表单堆叠。
- 页面树从纯列表推进到具备基础导航控制，暂不引入多级栏目和拖拽排序。
- 表单录入从“主要依赖后端兜底”推进到前端先做关键字段校验和友好错误提示。
- 重复 slug 从数据库原始错误推进到明确的业务冲突提示。
- 预览刷新和发布检查刷新已从分散调用收敛为统一派生状态刷新。
- 后台信息架构已从长页面推进到模块化站内导航，新用户可以从驾驶舱进入下一步操作。
- 已完成一次不上传图片的文字内容录入测试，文字内容链路正常。
- 预览入口已分层：模块内查看对应区域或详情页，最后通过“总体预览”查看完整官网。
- 预览体验已调整为站内当前窗口切换，不再默认新开标签页。
- 表单词条说明已开始补齐，降低用户第一次填写时的理解成本。
- 模块预览已进一步从“当前窗口展示完整页”修正为“当前窗口展示对应片段”。
- 表单输入区已开始区分“用户要填写的内容”和“系统固定显示的内容”，降低所有输入框风格一致造成的误解。
- 图片类字段已从技术化 URL 输入优化为素材选择交互，降低用户理解和操作成本。
- 媒体上传从“上传后还要保存”改为“上传即保存”，减少多余操作。
- 前台预览视觉从窄容器测试页推进到更开阔的官网预览样式。
- 前台官网预览已重做首页信息架构：顶部导航补充模块锚点和联系入口，Hero 更开阔，企业简介进入首屏后承接区。
- 联系电话、邮箱、地址已沉到底部页脚，符合常见企业官网浏览习惯。
- 服务、产品、案例、文章、FAQ 等模块已改为分区展示，并补充每个分区在最终网页中的用途说明。
- 后台内嵌预览高度已放大，减少左右和上下都被压缩的拘束感。
- 已修复预览模板重构后测试仍按旧 `hero` 标记截取导航区的问题。
- 后端测试和前端构建已通过。
- 前台官网预览已移除 Hero 右侧“网站结构”说明框，网站结构改由顶部导航和页面分区自然表达。
- 前台内容模块说明已收回到标题下方，不再占用右侧大块空白；内容卡片改为自适应网格，少量内容时也会更自然地填满横向空间。
- 站点身份表单已拆成“项目基础信息 / 官网顶部展示 / 搜索展示”三个分组，降低字段挤在一起造成的迷惑。
- 页面编辑表单已拆成“页面基础设置 / 页面正文 / 搜索展示”三个分组，导航、正文和 SEO 的职责更清楚。
- 文章编辑表单已拆成“文章基础信息 / 前台展示内容”两个分组，标题、短链接、封面、摘要和正文不再混在一块。
- 内容工作台编辑区新增浅色承载背景和分组留白，避免列表区和编辑区视觉上连成一片。
- 本轮后端测试和前端构建已通过。
- 前台首页锚点导航已补目标高亮，点击服务、产品、案例、资讯、FAQ、联系后对应导航项也会显示蓝色填充态。
- 前台内容分区和页脚已补吸顶导航偏移，点击导航跳转时不会被顶部导航栏压住。
- 前台锚点导航高亮逻辑已修正：点击服务、产品、案例、资讯、FAQ、联系时会取消首页蓝色填充，只高亮当前目标；键盘焦点改为描边，不再伪装成选中态。
- AWA 没有接入主链路，后续仍作为可选诊断能力。
- 当前还没有案例/服务/文章/产品字段级抽象复用，也还没有检索、权限、模板主题配置等进阶能力。
- 当前环境里 Vite dev server 绑定端口失败，已改用后端托管 `web/dist` 的方式演示。

### 下一步

- 当前 MVP 主流程可以先进入人工试用和问题修补阶段。
- 继续收尾时，优先处理明显缺陷、数据一致性问题和文档缺口。
- 后续增强再考虑内容检索/分页、权限登录、模板主题配置、发布回滚、素材分类和 AWA 可选诊断。
